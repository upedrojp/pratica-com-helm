apiVersion: v1
kind: Service
metadata:
  name: {{ include "ATV.fullname" . }}
  labels:
    {{- include "ATV.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ATV.selectorLabels" . | nindent 4 }}
